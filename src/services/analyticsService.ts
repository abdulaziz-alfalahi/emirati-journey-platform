
// Analytics service placeholder
export const analyticsService = {
  trackRoleSwitch: (previousRole: string | null, newRole: string) => {
    console.log('Role switch tracked:', { previousRole, newRole });
  }
};
